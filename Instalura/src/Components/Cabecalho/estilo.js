import {StyleSheet} from "react-native"

const estilo = StyleSheet.create({
    fotoUsuario:{
        width: 50,
        height: 50,
        borderRadius:30,
        margin:10,
    },
    cabecalho:{
        flexDirection:"row",
        alignItems:"center",
    }
})

export default estilo;